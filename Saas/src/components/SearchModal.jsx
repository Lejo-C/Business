import {useState} from "react";import Modal from "./Modal";import Icon from "./Icon";const items=["Overview","Leads","Clients","Projects","Tasks","Invoices","Analytics","Settings"];export default function SearchModal({onClose,setPage}){const[q,setQ]=useState("");const found=items.filter(x=>x.toLowerCase().includes(q.toLowerCase()));return <Modal title="Search workspace" onClose={onClose}><div className="relative"><Icon name="Search" size={16} className="absolute left-3 top-3 text-muted"/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} className="input pl-9" placeholder="Search pages..."/></div><div className="mt-3 space-y-1">{found.map(x=><button key={x} onClick={()=>{setPage(x);onClose()}} className="flex w-full items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium hover:bg-[#f0f1ea]"><Icon name="ArrowRight" size={15} className="text-muted"/>{x}</button>)}</div></Modal>}