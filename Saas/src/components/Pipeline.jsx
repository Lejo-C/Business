import {leads} from "../data/mock";import Icon from "./Icon";const stages=["Qualified","Discovery","Proposal","Negotiation"];export default function Pipeline({setPage}){return <div className="panel overflow-hidden"><div className="flex items-center justify-between border-b border-line p-5"><div><p className="eyebrow">Sales pipeline</p><h2 className="mt-1 text-lg font-bold tracking-tight">Deals this month</h2></div><button onClick={()=>setPage("Leads")} className="btn-ghost !px-3 !py-2 text-xs">View pipeline <Icon name="ArrowUpRight" size={14}/></button></div><div className="grid divide-y divide-line md:grid-cols-2 md:divide-x md:divide-y-0 lg:grid-cols-4">{stages.map(stage=>{const rows=leads.filter(x=>x.stage===stage);return <div key={stage} className="min-h-[190px] p-4"><div className="mb-4 flex items-center justify-between"><span className="text-xs font-semibold">{stage}</span><span className="rounded-md bg-[#f0f1ea] px-1.5 py-0.5 text-[10px] text-muted">{rows.length}</span></div>{rows.map(l=><div key={l.company} className="mb-2 rounded-xl border border-line bg-[#fafaf7] p-3 transition hover:border-ink/20 hover:bg-white"><div className="flex items-center gap-2"><span className="grid h-7 w-7 place-items-center rounded-lg bg-ink text-[9px] font-bold text-white">{l.initials}</span><p className="truncate text-xs font-semibold">{l.company}</p></div><div className="mt-3 flex items-center justify-between text-[11px]"><span className="font-bold">{l.value}</span><span className="text-muted">{l.age}</span></div></div>)}{!rows.length&&<div className="grid h-24 place-items-center rounded-xl border border-dashed border-line text-xs text-muted">No deals</div>}</div>})}</div></div>}