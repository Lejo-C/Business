import {useState} from "react";import {tasks} from "../data/mock";import Icon from "./Icon";export default function Tasks(){const[done,setDone]=useState([]);return <main className="animate-enter space-y-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="eyebrow">Work queue</p><h2 className="mt-2 text-3xl font-bold tracking-[-.04em]">Tasks</h2><p className="mt-2 text-sm text-muted">{tasks.length-done.length} tasks need action.</p></div><button className="btn-accent"><Icon name="Plus" size={16}/>New task</button></div><div className="panel divide-y divide-line">{tasks.map((t,i)=><div key={t.title} className={`flex items-center gap-4 p-4 transition hover:bg-[#fafaf7] ${done.includes(i)?"opacity-50":""}`}><button onClick={()=>setDone(d=>d.includes(i)?d.filter(x=>x!==i):[...d,i])} className={`grid h-6 w-6 shrink-0 place-items-center rounded-full border ${done.includes(i)?"border-ink bg-ink text-white":"border-[#c9cbc2]"}`}>{done.includes(i)&&<Icon name="Check" size={13}/>}</button><div className="min-w-0 flex-1"><p className={`text-sm font-semibold ${done.includes(i)?"line-through":""}`}>{t.title}</p><p className="mt-1 text-xs text-muted">{t.project} · {t.assignee}</p></div><span className={`hidden rounded-full px-2.5 py-1 text-[10px] font-bold sm:inline-flex ${t.priority==="High"?"bg-[#fbe6df] text-accent":"bg-[#f0f1ea] text-muted"}`}>{t.priority}</span><span className="w-20 text-right text-xs text-muted">{t.due}</span><Icon name="MoreHorizontal" size={16} className="text-muted"/></div>)}</div></main>}