export default function Table({columns,rows,renderRow}){return <div className="panel overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b border-line bg-[#fafaf7] text-[11px] uppercase tracking-wider text-muted"><tr>{columns.map(c=><th key={c} className="px-5 py-3.5 font-bold">{c}</th>)}</tr></thead><tbody className="divide-y divide-line">{rows.map((row,i)=>renderRow(row,i))}</tbody></table></div></div>}